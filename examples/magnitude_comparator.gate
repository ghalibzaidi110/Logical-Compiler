CIRCUIT MagnitudeComparator {
  INPUT A, B;
  OUTPUT A_GT_B, A_LT_B, A_EQ_B;
  WIRE notA, notB, xor1, notXor;
  notA = NOT(A);
  notB = NOT(B);
  A_GT_B = AND(A, notB);
  A_LT_B = AND(notA, B);
  xor1 = XOR(A, B);
  notXor = NOT(xor1);
  A_EQ_B = notXor;
}

