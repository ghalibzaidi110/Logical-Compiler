CIRCUIT MagnitudeComparator {
  INPUT A, B;
  OUTPUT A_GT_B, A_LT_B, A_EQ_B;
  WIRE notA, notB, xor1;
  notA = NOT(A);
  notB = NOT(B);
  A_GT_B = AND(A, notB);
  A_LT_B = AND(notA, B);
  xor1 = XOR(A, B);
  A_EQ_B = NOT(xor1);
}

