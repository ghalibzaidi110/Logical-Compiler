CIRCUIT Comparator1Bit {
  INPUT A, B;
  OUTPUT Equal, Greater, Less;
  WIRE notA, notB, xor1, notXor;
  notA = NOT(A);
  notB = NOT(B);
  xor1 = XOR(A, B);
  notXor = NOT(xor1);
  Equal = notXor;
  Greater = AND(A, notB);
  Less = AND(notA, B);
}

