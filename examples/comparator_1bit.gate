CIRCUIT Comparator1Bit {
  INPUT A, B;
  OUTPUT Equal, Greater, Less;
  WIRE notA, notB, xor1;
  notA = NOT(A);
  notB = NOT(B);
  xor1 = XOR(A, B);
  Equal = NOT(xor1);
  Greater = AND(A, notB);
  Less = AND(notA, B);
}

